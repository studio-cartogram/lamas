export class ApplicationBase {
    constructor(title) {
        this.title = title;
        this.bootstrap();
    }

    bootstrap() {
        // document.querySelector('.is-loading').classList.remove('is-loading');

    }
    
}
